# 2026-03-09

- 用户再次强调：我犯错后要记住，不能每次重复同类错误；要把错误和修正写进可持久化的记忆/规则。
- 定位相关新规则：不能凭坐标肉眼估行政区；凡是要给“具体位置/街道/地标”，默认先走高德逆地理编码，再基于返回结果作答。
- 本次纠正：曾把 `30.22319955063529, 120.26364242954095` 口算成西湖区，后经高德确认应为 **杭州市萧山区盈丰街道机场城市大道**。
- 摄像头相关新规则：不能把“当前宿主可调用的相机源”直接等同于“Mac 内建摄像头”；必须先区分成功抓取的是内建相机、iPhone 连续互通相机，还是桌上视角。
- 本次纠正：最初抓拍成功的两张图来自 **iPhone Continuity Camera / Desk View**，不是 Mac 内建摄像头；后续才单独打通并成功抓取 Mac 内建摄像头。
- 回复行为新规则：当用户已经确认收到内容（如回复“ok/嗯”）时，不能机械回 `NO_REPLY`；应给一句明确的自然确认，避免显得像出错或掉线。
- 摄像头权限表述新规则：对于 macOS 隐私提示里为何显示某个宿主（如 Cursor），默认表述为“可能是宿主归因/权限 UI 记账方式”，除非已经核实父进程和系统权限记录，否则不要说得像确定事实。
- 多智能体协同新规则：一旦启动多模型/多代理协作，必须先给用户发开工状态，至少包含：已启动、参与模型与分工、任务目标、预计下一次汇报时间；不能只在后台跑而不告知。
- 用户要求把“多 AI 协同”升级并正式命名为“工作室”。后续应统一按“工作室”表述，并按正规工作室/小公司的标准运行：有分工、流程、汇报、审核、验收、交付标准，不能敷衍。
- 用户进一步强调：线上线下都要一起改。不能只在聊天里改口，线上行为（开工/汇报/风险/结项/语气切换）和线下文档/Notion/流程都必须一起对齐成工作室模式。
- 用户新增要求：工作室对每个岗位/模型增加任务后评分机制。每次项目/任务做完后，由主智能体按 5 分制主观给各参与角色（包括主智能体自己）评分并记录，作为复盘和后续分工优化依据。
- 用户进一步要求：评分记录必须单独做成表，方便后续统计；不能只散落在项目正文里。主智能体以后每次项目结束都必须记分，不能忘。
- 用户新增汇报规则：每次定时汇报的范围按“上一次汇报之后”到“本次汇报时刻”为止来计算。以后只有早上 8:00 的定时汇报带新闻版；12:00 和 20:00 的定时汇报默认不带新闻段。8:00 的新闻版包含国内热点与国际综合新闻；其余时间段只汇报工作室这段时间发生的事、与用户聊天里发生的事，以及项目风险与下一步。除非用户临时单独要求，否则其他时段不补新闻。
- 热点新闻口径进一步明确：仅用于早上 8:00 的新闻版，且必须按两次汇报之间的时间窗统计；国内默认优先查实时热点源（如微博热搜、百度热搜、抖音热榜等），海外/国际新闻按 Brave 等检索做综合整理；若未完成相应源核查，不能草率写“本时段无明显新增热点新闻”。
- 时间承诺执行新规则：凡是对用户作出带明确时间点的承诺（如“10分钟后汇报”“半小时后提醒”“到点通知”），默认必须挂真实的定时触发/提醒机制，不能只靠口头记住；如果当下挂不上，必须当场明确告知用户，不能假装已经记住。
- 本次失败复盘：曾错误地把一个本地 `sleep 600` 后 `print` 到 stdout 的后台进程，当成“已经挂上可向用户主动发消息的真实提醒机制”。这其实只会在本地进程日志里输出文本，不会自动向当前聊天发送消息。后续凡是要做“到点主动给用户发消息”，必须确认定时机制具备真正的会话消息投递能力；仅有本地后台进程/打印日志不算完成。
- 本次验证结论：在当前 OpenClaw 环境里，带时间点的“到点主动发消息/主动汇报”可行方案是 `openclaw cron add --session isolated --announce`。已实测挂起 1 分钟验证任务与 4 分钟正式汇报任务并成功创建；后续默认走这条链路，并在正式依赖前先做一次短间隔投递验证。
- 工作室项目跟踪新规则：以后统一按“双轨状态”运行。Notion 负责项目底座（项目名、目标、分工、阶段、阻塞、下一步、最近确认状态），本地状态文件/日志/文件变更负责实时进度痕迹。后台 AI 一旦开工，必须至少留下一个可采样痕迹；定时汇报必须先读 Notion 项目底座，再做实时采样，再合成最终汇报。
- 工作室模式主动性新规则：不能再停留在“用户问什么才答什么”。主智能体需像工作室负责人一样主动推进、主动汇报、主动判断、主动留痕、主动纠偏；发现项目没实质进展、主线跑偏、流程失效或风险升高时，应主动拉回主线并同步，而不是等用户一层层追问。
- 工作室启动流程新规则：只要进入工作室模式，启动动作必须同步启动 Notion 流程。至少要在 Notion 项目总表中创建或更新项目底座，并写明项目名、目标、当前阶段、参与角色/分工、当前阻塞、下一步；同时主智能体必须尽快把任务真正拆给对应 AI，不能只在口头上报分工而不实际拆工执行。
- Qwen API 调试新结论：本机 `~/.zshrc` 已存在 `DASHSCOPE_API_KEY`，优先直接走 DashScope OpenAI 兼容接口，不再折腾 Qwen CLI。
- 本次脚本修正：`scripts/qwen_review.sh` 原先把 `curl` 输出直接 pipe 到 `python3 - <<'PY'`，由于 here-doc 占用了 stdin，导致解析阶段读不到 API 返回体并报 `JSONDecodeError`。后续凡是 shell + Python 解析返回值，避免这种写法；优先先接到变量/临时文件，再显式传给解析器。
- Word 模板套版新规则：当用户要求“格式完全一样”时，不能只改字号/行距等少量显眼项；默认要先解包 docx，对比模板与目标的 `styles.xml`、`settings.xml`、`numbering.xml`、`fontTable.xml`、`theme/theme1.xml`、`document.xml.rels`、页眉页脚与节设置，再决定是局部补丁还是整套骨架迁移。
- 本次环境结论：当前机器默认 `python3` 为 3.9，部分 docx 技能脚本用到 3.10+ 语法或缺少依赖（如 `defusedxml`）；必要时改用 `/usr/local/bin/python3.13`，若依赖仍缺失，可先用 ZIP 方式回包 OOXML，但要明确这属于环境受限下的替代方案。
