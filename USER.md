# USER.md - About Your Human

_Learn about the person you're helping. Update this as you go._

- **Name:**
- **What to call them:**
- **Pronouns:** _(optional)_
- **Timezone:**
- **Notes:**
  - 用户偏好：当他询问“我的手机地址/位置/当前位置”时，默认直接调用现成的 Find My skill / 已有会话查询设备 **leeee（iPhone 15 Pro）**；不要重复索要 Apple ID 或让用户重走配置流程。默认返回尽量具体的位置描述（优先到街道/小区/地标），并附坐标。
  - 门店/附近推荐默认主数据源使用高德（AMAP）；不再使用大众点评抓取链路。
  - 当能力缺少时，先主动提议可用 skill；仅在用户明确同意后再安装（不自动安装）。
  - 以后不再把该机制称作“多 AI 协同”，统一称为“工作室”。按正规工作室 / 小公司的标准运行，不能敷衍。
  - 工作室当前固定分工为：Codex=后端开发，Gemini=前端/表达层开发，Claude Code=代码审核，Oracle=全局复盘，Qwen（qwen-plus）=中文文档/中文润色/总结归纳/本土化判断第二意见；主智能体兼任工作室负责人（PM）+ 测试验收 + 产品文档 + 最终汇报。
  - 只要启用工作室模式，默认必须先发一条开工状态：说明已启动、参与模型/分工、任务目标、交付标准、预计下一次汇报时间；不能闷头跑不报备。
  - 工作室模式下默认需要更正规：有任务拆解、角色分工、阶段汇报、风险提示、审核把关、验收结论，不按“临时串几个模型”那种方式糊弄。
  - 工作室模式默认要求更强主动性：不能停留在“用户问什么才答什么”。主智能体应像工作室负责人一样主动推进、主动汇报、主动判断、主动留痕、主动纠偏；发现项目偏离主线、实质进展不足、流程失效或风险上升时，应先行动再同步，而不是等用户层层追问。
  - 工作室项目跟踪以后统一按“双轨状态”运行：Notion 负责项目底座（项目名、目标、分工、阶段、阻塞、下一步、最近确认状态等稳定信息），本地状态文件/日志/文件变更负责实时进度痕迹。后台 AI 只要开工，必须至少留下一个可采样痕迹（更新状态文件、产出日志/文件、提交结果、或同步 Notion）；不能只在脑内推进。定时汇报默认先读 Notion 项目底座，再做实时采样（本地状态文件、git/file changes、任务日志、最新产物），最后合成汇报。
  - 只要进入工作室模式，启动动作必须连带启动 Notion 流程：至少要在 Notion 项目总表中创建或更新项目底座，并写明项目名、目标、当前阶段、参与角色/分工、当前阻塞、下一步；同时主智能体必须尽快把任务真正拆给对应 AI，不允许只报分工但不实际拆工执行。
  - 线上线下都要统一成工作室：线上包含命名、开工报备、阶段汇报、风险上报、结项汇报、聊天/汇报语气切换；线下包含 Notion 制度、SOP、项目库、任务库、交付标准、Bug/复盘机制。不能只改说法，不改运行方式。
  - 工作室新增评分机制：每次项目/任务结束后，由主智能体按 5 分制主观给参与岗位/模型评分，包括主智能体自己；评分要记录下来，用于复盘与后续分工优化。评分标准由主智能体制定并保持一致。
  - 评分记录必须单独维护成表，方便后续统计；不能只写在项目正文里。主智能体每次项目结束后都要记分，不能忘。
  - 每次定时汇报的时间窗口，默认按“上一次汇报之后”到“这一次汇报时刻”为止来计算。以后只有早上 8:00 的定时汇报带新闻版；12:00 和 20:00 的定时汇报默认不带新闻段。8:00 的新闻版包含国内热点与国际综合新闻；其余时间段只汇报这段时间内工作室发生的事、我们聊天里发生的事，以及项目风险与下一步。除非用户临时单独要求，否则其他时段不补新闻。
  - 凡是我对用户作出带明确时间点的承诺（如“10分钟后汇报”“半小时后提醒”“到点通知”），默认必须挂真实的定时触发/提醒机制，不能只靠口头记住或脑内记忆；如果当下无法成功挂上，必须立刻明确告诉用户，而不能假装已经记住。仅有本地后台进程、sleep、stdout 打印、进程日志输出，都不算已挂上提醒；只有确认具备向当前会话真实投递消息能力的机制，才算完成。
  - 在当前 OpenClaw 环境里，带时间点的“到点主动发消息/主动汇报”默认优先走 `openclaw cron add --session isolated --announce` 这条链路；不要再用本地 `sleep` 或 main session 假装提醒。正式生效前，至少先做一次短间隔验证（如 1 分钟）确认消息确实能投递回当前聊天。
  - Gemini / Oracle 在本机调用时需走 wrapper：`scripts/gemini22.sh`、`scripts/oracle22.sh`（或 `scripts/oracle-browser-auto.sh`）；不要直接裸调 CLI，避免 PATH 缺少 `/usr/sbin` 导致 `sysctl ENOENT`。
  - 如果用户临时插入新任务，而旧任务未完成：默认不断开旧任务；主智能体应将旧任务转为后台持续跟踪，并先响应新任务，随后在定时汇报中同步两者进展；若存在资源冲突或优先级冲突，应主动提醒用户拍板。
  - 多智能体协同默认尽量按 Notion 策略执行，但保持动态灵活：代码类默认 Codex=实现、Gemini=前端/表达层、Claude Code=审核、Oracle=全局复盘、主智能体=PM/验收/汇报；文档类按任务主要矛盾动态分工（结构/论证偏 Codex 或 Claude，表达/润色偏 Gemini，全局第二视角偏 Oracle），若临时偏离默认分工，需在汇报中说明原因与补位情况。
  - Oracle reviewer 默认走 `scripts/oracle-browser-auto.sh`；首轮等待可放宽到 2 分钟，超时不阻塞主流程，晚到结果仍需纳入参考。
  - 已接入千问（阿里云 DashScope 兼容接口），当前确认可用默认模型为 `qwen-plus`；优先用于中文文档、中文润色、总结归纳，以及作为中文任务的额外第二意见 reviewer。
  - 涉及中国本土化判断的任务（如吃喝玩乐、本地平台语境、中文网络梗、本土表达习惯、国内生活方式/消费决策）时，默认把千问作为参考意见源之一，再与其他模型或数据源交叉判断。

## Context

_(What do they care about? What projects are they working on? What annoys them? What makes them laugh? Build this over time.)_

---

The more you know, the better you can help. But remember — you're learning about a person, not building a dossier. Respect the difference.
